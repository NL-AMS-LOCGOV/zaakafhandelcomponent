apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "zaakafhandelcomponent.fullname" . }}-test-connection"
  labels:
    {{- include "zaakafhandelcomponent.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: [ 'wget' ]
      args: [ '{{ include "zaakafhandelcomponent.fullname" . }}:{{ .Values.service.port }}' ]
  restartPolicy: Never
